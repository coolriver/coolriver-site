/**
 * 常量配置
 */

 // 响应式布局不同尺寸下，index页面中文章卡片点位宽度配置，按照文章索引依次取循环地取值
export const cardFlexLayout = {
  xs: [12],
  md: [6, 6, 12],
  lg: [3, 3, 3, 6, 6, 5, 7]
};

// 最近文章模块展示的文章数量
export const RECENT_ARTICLE_COUNT = 4;
